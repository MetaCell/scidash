version: '2'

services:
  scidash-redis:
    image: redis
  scidash-postgres:
    image: metacell/scidash-db
  scidash:
    image: metacell/scidash:1.2.0
    ports:
        - 80:8000
    depends_on:
        - scidash-redis
        - scidash-postgres

